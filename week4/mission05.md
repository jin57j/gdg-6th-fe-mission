https://hayoungarchive.tistory.com/25#comment15817694

1. 배울 점 단순히 useEffect 사용법만 나열한 것이 아니라, useState만 사용했을 때 발생하는 무한 루프 대참사의 원인과 렌더링 사이클의 흐름을 단계별 코드 실행 -> fetch -> setState -> 리렌더링 -> 다시 fetch 로 명확하게 시각화되어있다 . 또한 실제 미션에서의 구현과정이 명확하게 표현되어있다.

2. 아쉬운 점 useEffect 설명 중 '데이터가 오기 전 화면을 띄운다'고 언급했으나, 비동기 통신에서 필수적인 상태 관리 변수가 실제 프로젝트 코드나 구조에 어떻게 녹아들었는지 구체적인 컴포넌트 단의 구현 힌트가 누락되어 아쉬움이 남는다.

3. 느낀점 제시된 흐름에 따라 작성된 좋은 글인 것 같다 또한 느낀점에서는 공감이 일부 가는 부분이 존재 했다.

https://velog.io/@yeonseo_/FE-%EB%AF%B8%EC%85%98-%EC%BD%94%EC%8A%A4-3%EC%A3%BC%EC%B0%A8

1. 배울 점 비동기 개념의 명확한 대조: 동기식 코드와 비동기식 코드의 실행 차이를 단순한 이론 설명에 그치지 않고, console.log의 출력 순서 예시를 통해 독자가 직관적으로 이해할 수 있었다. 전체적으로 간단한 코드와 설명을 병기하여 독자로 하여금 어떻게 쓰이는지 직관적인 이해를 이끌어낸다.

2. 아쉬운 점 에러 핸들링 로직의 부재: fetch와 axios를 비교하면서 데이터 파싱(response.json())의 차이점은 잘 짚어냈지만 실제 네트워크 요청이 실패했을 때에 대한 대비가 빠져 있다. try...catch 문을 도입해 에러를 잡아내는 코드가 productApi.js나 useEffect 내부에 추가되었다면 더 좋은 코드가 되었을 것이 다.

3. 느낀 점 (Reflection) 기술의 필요성을 명확히 인지하고 구현 흐름을 스스로 정리해 나가는 이 과정 이 잘드러나있어 읽기에 좋은 글이었다.
